package com.ali.whatsappplus.viewmodel

import androidx.lifecycle.ViewModel
import androidx.lifecycle.viewModelScope
import com.ali.whatsappplus.util.Constants
import com.cometchat.chat.models.TextMessage
import kotlinx.coroutines.launch

class ChatViewModel : ViewModel() {

//    val messageList = mutableListOf<TextMessage>()
//
//    fun addToChat(textMessage: TextMessage, source: String) {
////        viewModelScope.launch {
////            messageList.add(textMessage.text, textMessage.sender)
////        }
//    }
//
//    fun addResponse(response: TextMessage) {
//        addToChat(response, Constants.RECEIVED)
//    }
}